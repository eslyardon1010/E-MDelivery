import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
   red: '#FF0808',
   black: '#000000', 
   white: '#FFFFFF', 
   redDark: '#8D1515', 
   redLight: ' #EF5757'
  },
};

export default theme;